<div class="col-xs-5">

        <%= render 'dashboard/trends_nav' %>


</div>




<div class="col-xs-7">

<div>
<div class="btn-group chartType">
    <button ng-click="updateChartNumber('1')" class="btn btn-default">
        <span class="glyphicon glyphicon-align-left"></span>
    </button>
    <button ng-click="updateChartNumber('2')" class="btn btn-default">
        <span class="glyphicon glyphicon-th-list"></span>
    </button>
    <button ng-click="updateChartNumber('3')" class="btn btn-default">
        <span class="glyphicon glyphicon-adjust"></span>
    </button>
</div>
</div>

     <div ng-show="report_type=='vendor' && chart_number==1">

        <div google-chart chart="vendorChart1" style="{{cssStyle}}" on-ready="chartReady()"></div>

        <div google-chart chart="vendorChart1b" style="{{cssStyle}}" on-ready="chartReady()"></div>

     </div>

    <div ng-show="report_type=='vendor' && chart_number==2">
            <div google-chart chart="vendorChart2" style="{{cssStyle}}" on-ready="chartReady()"></div>
    </div>

    <div ng-show="report_type=='vendor' && chart_number==3">
        <div google-chart chart="vendorChart3" style="{{cssStyle}}" on-ready="chartReady()"></div>
    </div>


    <div ng-show="report_type=='price_breakdown' && chart_number==1" >Price Breakown Chart one
        <div google-chart chart="priceChart1" style="{{cssStyle}}" on-ready="chartReady()"></div>
    </div>
        <div id="chart_div" style="width: 900px; height: 500px;"></div>

    <div ng-show="report_type=='price_breakdown' && chart_number==2">Price Breakown Chart two
    </div>

         <div id="calendar_basic" style="width: 900px; height: 500px;"></div>

    <div ng-show="report_type=='hotspots' && chart_number==1" >hotspots Chart one
    </div>

    <div ng-show="report_type=='hotspots' && chart_number==2">hotspots Chart two</div>


    <div ng-show="report_type=='days_prior' && chart_number==1" >days_prior Chart one</div>

    <div ng-show="report_type=='days_prior' && chart_number==2">days_prior Chart two</div>


</div>


